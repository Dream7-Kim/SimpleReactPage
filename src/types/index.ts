export enum App_Theme {
	light = 'light',
	dark = 'dark'
}

export enum Login_Type {
	Email = 2,
	Wallet = 3
}

export enum Env {
	development = 'development',
	production = 'production'
}

export enum Mode {
	test = 'test',
	prod = 'prod'
	// main = 'main'
}
